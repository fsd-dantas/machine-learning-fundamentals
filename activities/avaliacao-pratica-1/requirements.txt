# Avaliação Prática 1 — deep learning stack.
# Colab already ships TensorFlow, PyTorch and torchvision with matching CUDA builds;
# pinning them here would fight the runtime, so they are left to the environment and
# only the missing pieces are installed.

tensorflow>=2.15
torch>=2.1
torchvision>=0.16
# transformers 5.x refactored the ViT implementation and renamed every encoder
# parameter (encoder.layer.N.attention.attention.query -> vit.layers.N.attention.q_proj).
# Loading `google/vit-base-patch16-224-in21k` under 5.x silently discards the whole
# pretrained encoder as UNEXPECTED and randomly initialises the model instead — the
# script still runs, still reports a number, and the number is meaningless.
# s5_vit.py asserts against this regardless of version; the pin keeps it from happening.
transformers>=4.40,<5   # ViT (strategy 5)
scikit-learn>=1.3
scipy>=1.11
numpy>=1.24
matplotlib>=3.7         # confusion matrix
datasets>=2.14         # CIFAR-10 via HuggingFace CDN — cs.toronto.edu throttles some networks to ~30 KB/s
